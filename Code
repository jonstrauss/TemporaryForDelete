

If DEBUG_SHOW_ACTIVE_TITLE Then
  Dim awt, tmsg
  awt = GetActiveWindowTitle()
  If Len(awt) = 0 Then
    tmsg = "(unknown / PowerShell blocked)"
  Else
    tmsg = """" & awt & """"
  End If
  MsgBox "Active window title is:" & vbCrLf & tmsg, vbInformation, "LoadClipSection — Debug"
End If
